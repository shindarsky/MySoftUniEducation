function solve(numberOfPages, pagesRead, dayReading) {
    let totalTime = numberOfPages / pagesRead;
    let hoursPerDay = totalTime / dayReading;

    console.log(hoursPerDay);
}

solve(432,
    15 ,
    4 
    );